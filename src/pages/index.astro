---
import PostLayout from "../layouts/PostLayout.astro"

const post = (await Astro.fetchContent<Post>("./posts/*.md"))
  .sort((a,b) => (a.published < b.published) ? 1 : -1)[0]
---
<PostLayout content={post} />

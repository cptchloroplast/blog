---
import BaseLayout from "./BaseLayout.astro"
import Post from "../components/Post.astro"

export interface Props {
  content: FetchContentResult<Post>
}

const { content } = Astro.props as Props
const { title } = content
---
<BaseLayout title={title}>
  <Post post={content} />
</BaseLayout>
